// Placeholder repository for notifications
export class NotificationsRepository {
    static getNotifications(userId) {
        return new Promise((resolve, reject) => {
            // Placeholder implementation
            resolve([]);
        });
    }
}